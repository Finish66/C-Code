include<stdio.h>
#include<unistd.h>
#include<fcntl.h>

int main(){
	int fd, SIZE = 1024;
	off_t fileSize, offset;
	ssize_t byteRead, byteWritten;
	char buf[SIZE];


	int fd = open("half.txt", O_RDONLY);
	off_t fileSize = lseek

	char buf[SIZE/2];
	int c, offSet;
	scanf("%d", &c);
	
	if(c == 1){
			
	}
	else if(c == 2){
	
	}
	else{
		printf("Invalid Option.\n");
	}
	read()

TZ
